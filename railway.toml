[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm build:bot"

[deploy]
startCommand = "node packages/bot/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[volumes]
mountPath = "/data"

[[services]]
name = "nexus-bot"
