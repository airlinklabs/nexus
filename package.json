{
  "name": "nexus",
  "private": true,
  "scripts": {
    "start": "node packages/bot/dist/index.js",
    "dev:bot": "pnpm --filter bot dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build:bot": "pnpm --filter bot build",
    "build:dashboard": "pnpm --filter dashboard build",
    "build": "pnpm build:bot && pnpm build:dashboard",
    "deploy:commands": "pnpm --filter bot deploy:commands",
    "test": "pnpm --filter bot test",
    "test:watch": "pnpm --filter bot test:watch"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
